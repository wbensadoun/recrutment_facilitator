# Configuration de la base de données
DB_HOST=${DB_HOST_PROD:-localhost}
DB_PORT=${DB_PORT_PROD:-5432}
DB_USER=${DB_USER_PROD:-postgres}
DB_PASSWORD=${DB_PASSWORD_PROD:-postgres}
DB_NAME=${DB_NAME_PROD:-recruitment_facilitator}

# Variables de production PostgreSQL
POSTGRES_USER_PROD=postgres
POSTGRES_PASSWORD_PROD=postgres
POSTGRES_DB_PROD=recruitment_facilitator
POSTGRES_HOST_PROD=postgres
POSTGRES_PORT_PROD=5432

# Configuration des URLs
API_HOST=${API_HOST_PROD:-localhost}
API_PORT=${API_PORT_PROD:-3000}
FRONTEND_HOST=${FRONTEND_HOST_PROD:-localhost}
FRONTEND_PORT=${FRONTEND_PORT_PROD:-8000}

# URLs dynamiques basées sur les variables
REACT_APP_API_URL=http://${API_HOST}:${API_PORT}/api
VITE_API_URL=http://${API_HOST}:${API_PORT}/api
NEXT_PUBLIC_API_URL=http://${API_HOST}:${API_PORT}/api

# Configuration SMTP (à remplacer par vos informations)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# JWT Secret for local development
RESET_SECRET=your-jwt-secret-key-for-development

# Database URL for local development
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/recruitment_facilitator

# Additional missing variables to prevent warnings
SMTP_SECURE_PROD=false
SMTP_FROM_PROD=noreply@candidater-france.com
RESET_SECRET_PROD=your-jwt-secret-key-for-production

# Configuration de l'application
NODE_ENV=${NODE_ENV_PROD:-development} # Default to development
PORT=3002

# URL de l'application (utilisée dans les liens d'email)
APP_URL=http://${FRONTEND_HOST}:${FRONTEND_PORT}
NEXT_PUBLIC_APP_URL=http://${FRONTEND_HOST}:${FRONTEND_PORT}

# Secret pour la réinitialisation du mot de passe
RESET_SECRET=${RESET_SECRET_PROD} # REQUIS