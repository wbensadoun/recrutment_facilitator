# Exemple de configuration pour le déploiement
# Copiez ce fichier vers .env et modifiez les valeurs selon votre environnement

# ===========================================
# CONFIGURATION POUR PRODUCTION
# ===========================================

# Hosts et ports (modifiez selon votre serveur)
API_HOST_PROD=localhost
API_PORT_PROD=3000
FRONTEND_HOST_PROD=localhost
FRONTEND_PORT_PROD=8000

# Base de données PostgreSQL
DB_HOST_PROD=postgres
DB_PORT_PROD=5432
DB_USER_PROD=postgres
DB_PASSWORD_PROD=your-secure-password-here
DB_NAME_PROD=recruitment_facilitator

POSTGRES_USER_PROD=postgres
POSTGRES_PASSWORD_PROD=your-secure-password-here
POSTGRES_DB_PROD=recruitment_facilitator
POSTGRES_HOST_PROD=postgres
POSTGRES_PORT_PROD=5432

# Configuration SMTP pour l'envoi d'emails
SMTP_HOST_PROD=smtp.gmail.com
SMTP_PORT_PROD=587
SMTP_USER_PROD=your-email@domain.com
SMTP_PASSWORD_PROD=your-app-password
SMTP_FROM_PROD=noreply@your-domain.com
SMTP_SECURE_PROD=false

# Sécurité
RESET_SECRET_PROD=your-very-secure-jwt-secret-key-for-production

# ===========================================
# EXEMPLES DE CONFIGURATION SELON L'ENVIRONNEMENT
# ===========================================

# Pour un serveur avec domaine personnalisé :
# API_HOST_PROD=api.your-domain.com
# FRONTEND_HOST_PROD=your-domain.com
# API_PORT_PROD=443
# FRONTEND_PORT_PROD=443

# Pour un serveur avec IP publique :
# API_HOST_PROD=192.168.1.100
# FRONTEND_HOST_PROD=192.168.1.100
# API_PORT_PROD=3000
# FRONTEND_PORT_PROD=8000

# Pour Docker Swarm ou Kubernetes :
# API_HOST_PROD=backend-service
# FRONTEND_HOST_PROD=frontend-service
